/** Board cell pitch in design units. Blocks are slightly smaller to leave a gap. */
export const CELL = 80;
export const BLOCK = 72;

/** How far above the finger a dragged piece floats, so the finger never hides it. */
export const DRAG_LIFT = CELL * 1.2;
